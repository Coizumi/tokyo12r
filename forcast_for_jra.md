# TOKYO12R JRA Forecast Logic

対象実装は `scripts/jra_site_updater.py` である。この文書は予想ロジックのみを定義し、インフラ、更新、デプロイの運用は `specs.md` に記載する。

## ロジック概要

TOKYO12RはJRA公式出走表から取得した出走馬情報と近走テキストを用い、各レース最大5頭へ `◎ ○ ▲ △ ☆` を付ける。乱数や機械学習モデルは使わない。

すべてのレースで、近走データが1走から4走のどの状態でも同じ統一スコア方式を使う。全馬に近走4走がそろった場合だけに使う別の持ちタイム・末脚・先行力の正規化経路は廃止した。

現行オッズ、斤量、騎手、性齢、天候、調教、厩舎コメント、新聞印は印決定に使わない。現行人気は表示用に保持し、近走テキストに含まれる過去人気だけを基礎スコアへ使う。

## 入力

直接スコアに使う情報は次のとおり。

- 馬番、枠番
- 父名、母の父名
- 取得できた最大4走分の近走テキスト
- レースの競馬場、芝・ダート・障害、距離、馬場状態、レース名

近走テキストからは着順、頭数、過去人気、括弧内の着差相当値、距離を抽出する。獲得賞金は出走表から読める場合もあるが、予想スコアおよび印決定には一切使わない。

## 近走の基礎スコア

近走は新しい順に、取得できた分だけ次の重みを使う。4走未満でも欠損を補わず、存在する近走だけを加算する。

```text
1走前: 1.00
2走前: 0.95
3走前: 0.90
4走前: 0.85
```

各近走の加点は以下である。

```text
着順・頭数 = max(0, (頭数 + 1 - 着順) / 頭数) * 8.0 * 近走重み
3着以内    = (4 - 着順) * 1.4 * 近走重み
過去人気   = max(0, 18 - 人気) * 0.12 * 近走重み
着差相当   = max(0, 2.0 - 着差) * 0.75 * 近走重み
```

最後に同点時の並びを安定させるため、馬番へ以下の微小補正を加える。

```text
(19 - 馬番) * 0.015
```

この基礎スコアは勝率や期待値ではなく、馬どうしを並べるための指数である。

## クラス相対ボーナス

各馬の近走最大4走から最も高いレース格を一つ採り、出走馬内で順位化する。近走の重み自体はクラスで変更しない。

| レース格 | クラススコア |
| --- | ---: |
| GI | 0.60 |
| GII | 0.50 |
| Jpn1 | 0.45 |
| GIII | 0.40 |
| OP / Listed | 0.30 |
| 3勝クラス | 0.22 |
| Jpn2 | 0.18 |
| 2勝クラス | 0.12 |
| Jpn3 | 0.08 |
| 1勝クラス以下、グレード不明重賞 | 0.00 |

該当近走が6着以下ならそのクラススコアを半減する。対象レースがGI・GII・GIIIなら、近走の3勝・2勝クラスはともに `0.00` とする。

レース内の高位3段階へ、同点同順位で `class_rank_bonus` を加える。

```text
1位: +6.0
2位: +4.0
3位: +2.0
その他またはクラススコア0: +0.0
```

## 血統レース条件適性

`data/Sire_data.csv` の父・母の父データを使う。芝を `100`、ダートを `-100` として、種牡馬の `surface_axis` と距離の近さから単体適性を求める。

```text
surface_fit = 1 - min(abs(surface_axis - race_surface_axis), 200) / 200
distance_fit = 1 - min(abs(distance_m - race_distance_m), 600) / 600
lineage_fit_score = (surface_fit * 0.55 + distance_fit * 0.45) * 100

sire_fit_score = sire_lineage_fit_score
  + max(0, dam_sire_lineage_fit_score - 50) * 0.35
```

未登録または距離不明の系統は中立値 `50.0`、最終値の上限は `100.0` とする。母の父は中立値超過分だけを父の評価へ加点し、減点には使わない。

### 重・不良馬場の種牡馬加点

`data/muddy_sire_bonus.json` は、2026-09-12に確認した[芝の記事](https://omokeiba.blog.jp/archives/96500810.html)と[ダートの記事](https://omokeiba.blog.jp/archives/96495353.html)の分類を構造化した参照データである。芝またはダートで公式出馬表の馬場状態が `重`・`不良` の時だけ、該当する父へ直接加点する。`稍重`、障害、馬場状態またはコース種別が不明な場合、母の父には適用しない。

```text
muddy_sire_bonus = min(1.8, data.bonus_points)
```

性別条件付きの父系は該当する牡馬・牝馬だけに適用し、1頭あたりの加点上限は `+1.8` とする。

## 競馬場コース補正

`course_bias_score` は阪神、京都、福島の競馬場・距離固有の加算値で、対象外と障害は `0.0` とする。枠番を取得できる場合は1枠を内、8枠を外として扱い、取得できない場合だけ馬番を出走頭数で正規化して代用する。距離短縮は直近走の距離が今回より長い場合に適用する。

統一スコア方式では持ちタイム・末脚・先行力の別指数を算出せず、コース補正にも使わない。枠順、距離短縮、父系など取得可能な条件だけを補正へ使う。

### 阪神

2026-09-04に確認した[阪神競馬場のコース解説動画](https://youtu.be/gzwgzVX2vbI?si=prk5JtczWG60vdUV)を根拠とする。

- 芝1200m、1600m、1800m、2000m、2400m: 内枠を評価する。
- 芝1400m: 前走1600m以上の距離短縮馬へ、短距離からの距離延長馬が出走馬の3分の1以上いる時だけ `+2.0`。
- ダート全距離: 外枠を評価し、1400m・2000mは外枠評価を強める。

### 京都

2026-09-04に確認した[京都競馬場のコース解説動画](https://youtu.be/Z70bmeAETY8?si=K1Kk7xelbvnCxxyp)を根拠とする。

- 芝1200m、2000m、3000m以上: 内枠を評価する。
- 芝1400m、1600m、1800m、2200m: 距離短縮馬を評価する。
- ダート1400m: 外枠と距離短縮を評価する。
- ダート1800m・1900m: 内枠を評価する。

### 福島

2026-09-04に確認した[福島競馬場のコース解説動画](https://youtu.be/cgUpqnbR250?si=lDif4rBK5k9k0Wnw)を根拠とする。芝1200m・1800mは、コース情報にAコースが明示される時は内枠、Bコースが明示される時は外枠を評価する。明示がなければ補正しない。

- 芝2000m: 父キズナへ `+1.5`。
- 芝2600m: 父オルフェーヴルまたはゴールドシップへ `+2.0`。
- ダート1700m: 距離短縮馬を評価する。

## 統一スコアと印

すべてのレースで、全頭指数と予想ページの指数バッジには同じ統一スコアを表示する。

```text
unified_score = base_score
  + sire_fit_score * 0.08
  + class_rank_bonus
  + course_bias_score
  + muddy_sire_bonus
```

スコアを降順、同点なら馬番昇順、さらに馬名昇順で並べ、上位から `◎`、`○`、`▲`、`△`、`☆` を順に付ける。近走4走がそろっているかどうかで式、指数、印の付け方は変わらない。

## 買い目と的中判定

印が5頭そろう場合だけ、2026-08-15以降は次を作る。

- ワイドBOX: `○▲△` の3点。各組み合わせの2頭が1着から3着に入れば的中し、複数的中は払戻額を個別に表示する。
- 3連複BOX: `◎○▲△☆` の10点。
- 3連単フォーメーション: `◎○ - ◎○▲ - ◎○▲△☆` の12点。

結果確定後、公開済みの印と1着・2着・3着を照合して式別に判定し、取得できる払戻を表示する。過去開催分は当時の買い目定義を維持する。

## 制約

- 指数は予測確率や期待値ではない。
- 現行オッズと現行人気は印決定に使わない。
- 斤量、騎手、性齢、天候、調教、厩舎コメントは使わない。
- 獲得賞金は予想スコアに使わない。
- 近走テキストの構造変化や、`Sire_data.csv` に未登録の血統は評価精度を下げうる。
